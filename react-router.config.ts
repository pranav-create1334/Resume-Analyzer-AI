import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // Server-side render is enabled so Gemini requests can run securely on the server.
  ssr: true,
} satisfies Config;
